/* чтоб создать подложку:делаем в родительском эл position:relative  а в дочернем position:absolute  и записываем его .ourClass:before{...} вот пример:
.card {
  position: relative;
  border-radius: 20px;
  overflow: hidden;
  border-radius: 20px;
  padding: 32px;
}
.card:before {
  content: "";
  position: absolute;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(0deg, rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.3));
}
*/

/* чтоб добавить комментарий после тега записываем его так: tag.className|c жмяк */

 /* чтоб обрезалась картинка по размеру тега object-fit: cover; */

   /* чтоб спрятать то, что выпирает (тут уголки из-за скругления): */
  overflow: hidden;

чтоб заполняли ряд, а потом переносились на след строку
  .delivery-cards {
  display: flex;
  flex-wrap: wrap;
}